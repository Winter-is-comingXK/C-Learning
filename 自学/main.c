#include <stdio.h>

void pr(int m, int n) {
    int kongge = m - 2;
    printf("X");
    for (int line = 1; line <= (m + 1) / 2; line++) {
        for (; lin <=; ++i) {
            printf("\040");
        }
    }
    pr
}


int main() {

    int m, n;
    scanf("%d%d", &m, &n);
    pr(m, n);

}
